#ifndef CDC_BASE_HPP
#define CDC_BASE_HPP

namespace CDC {

    // constants
    // -----------------------------------------------------------------------
    const unsigned short ADMIN_LEVEL      = 0;
    const unsigned short ANALYST_LEVEL    = 1;
    const unsigned short TESTER_LEVEL     = 2;
    const unsigned short GUEST_LEVEL      = 3;
    const unsigned short BAN_LEVEL        = 10;
    // -----------------------------------------------------------------------

}



#endif // CDC_BASE_HPP
